import React from 'react'
import styles from '@/app/dashboard.module.css'

const MainSection = () => {
  return (
    <div className={`flex-[40rem] p-4 ${styles['box-shadow']}`}>MainSection</div>
  )
}

export default MainSection